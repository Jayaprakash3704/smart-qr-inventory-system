# Admin Bootstrap Credentials
# Read by the server on startup. Creates the user in Firebase and assigns 'admin' role in SQLite.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Firebase Admin Configuration
FIREBASE_PROJECT_ID=qr-app-inventory-sys-24
